# Entity: nearest

main: kit: entity: nearest: {

  alias: field: {}

  fields: [
    {
      name: "result"
      req: true
      short: "Array of nearest postcodes sorted by distance"
      type: "`$ARRAY`"
    }
    {
      format: "int32"
      name: "status"
      req: true
      type: "`$INTEGER`"
    }
  ]

  name: "nearest"
  op: {
    list: {
      name: "list"
      points: [
        {
          args: {
            params: [
              {
                example: "SW1A 2AA"
                kind: "param"
                name: "postcode_id"
                orig: "postcode"
                reqd: true
                type: "`$STRING`"
              }
            ]
          }
          method: "GET"
          orig: "/postcodes/{postcode}/nearest"
          rename: {
            param: {
              postcode: "postcode_id"
            }
          }
          segments: [
            {
              lit: "postcodes"
            }
            {
              var: "postcode_id"
            }
            {
              lit: "nearest"
            }
          ]
          select: {
            exist: [
              "postcode_id"
            ]
          }
          transform: {
            req: "`reqdata`"
            res: "`body.result`"
          }
        }
      ]
    }
  }

  relations: {
    ancestors: [
      [
        "postcode"
      ]
    ]
  }



}
